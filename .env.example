SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Injected by the Vercel Supabase integration. The Vercel Production build uses
# this direct connection to apply migrations; Preview builds never use it.
POSTGRES_URL_NON_POOLING=

# Required in every deployed environment that exposes cron routes. Vercel
# automatically sends this value as an Authorization bearer token.
CRON_SECRET=

# Public Vercel Blob store used to mirror retailer product images.
# This is injected automatically after linking a Blob store to the project.
BLOB_READ_WRITE_TOKEN=

# Optional: the anonymous Woolworths site defaults to Glenfield (store 9171).
# Set a browser session cookie only when you intentionally select another store.
WOOLWORTHS_COOKIE=
WOOLWORTHS_STORE_CITY=Auckland
WOOLWORTHS_MAX_PAGES=60
WOOLWORTHS_PAGE_SIZE=100

# The first PAK'nSAVE collector defaults to the real Royal Oak store.
# Prefer STORE_ID for an exact, stable selection when changing stores.
PAKNSAVE_STORE_ID=e1925ea7-01bc-4358-ae7c-c6502da5ab12
PAKNSAVE_STORE_QUERY=Royal Oak
PAKNSAVE_STORE_CITY=Auckland
PAKNSAVE_MAX_PAGES=20

# Maximum number of top deals retained per retailer in data/deals.json.
LOCAL_DEALS_PER_RETAILER=100

# Used for absolute social-preview URLs.
SITE_URL=http://localhost:3000
